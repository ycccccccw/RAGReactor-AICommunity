# 阶段 4 验收记录

- 编译：`make server` 通过。
- 自动测试：`make test` 已通过，共 7 组（API、RAG 阶段 2、Prompt、SSE、韧性、混合检索、推荐排序）。
- 默认兼容：`include_community` 默认关闭，知识检索路径与缓存策略保持原样。
- 隔离：知识库和社区继续使用独立 `.ragvec` 文件，仅在请求时合并候选。
- 可信度：API、页面和 Prompt 三处均区分知识资料与社区经验。
- 删除安全：相关内容与问答仅加载社区索引中 `status=ready` 的内容；阶段 2 删除/屏蔽同步规则继续生效。
